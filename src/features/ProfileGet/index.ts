import { profileReducer, profileActions } from './model/slices/profileSlice';
import { Profile, ProfileSchema } from './model/types/profile.types';

export { profileReducer, profileActions };
export type { ProfileSchema, Profile };
